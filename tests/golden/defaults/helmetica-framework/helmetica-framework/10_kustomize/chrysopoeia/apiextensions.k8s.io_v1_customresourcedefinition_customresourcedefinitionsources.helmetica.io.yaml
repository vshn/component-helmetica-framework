apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: customresourcedefinitionsources.helmetica.io
spec:
  group: helmetica.io
  names:
    kind: CustomResourceDefinitionSource
    listKind: CustomResourceDefinitionSourceList
    plural: customresourcedefinitionsources
    singular: customresourcedefinitionsource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.reference.name
      name: SourceRef
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: CustomResourceDefinitionSource is the Schema for the bundlesources
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CustomResourceDefinitionSourceSpec defines the desired state
              of CustomResourceDefinitionSource.
            properties:
              crdNames:
                description: |-
                  CRDNames is the `.spec.names` field of the generated [apiextv1.CustomResourceDefinition].
                  If not set takes the name from the chart's metadata or falls back to the default `Instance` kind.
                  Please note that changing this field generates a new CRD and does not update the existing one.
                  The old CRD will be left in the cluster and must be removed manually.
                properties:
                  categories:
                    description: |-
                      categories is a list of grouped resources this custom resource belongs to (e.g. 'all').
                      This is published in API discovery documents, and used by clients to support invocations like
                      `kubectl get all`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  kind:
                    description: |-
                      kind is the serialized kind of the resource. It is normally CamelCase and singular.
                      Custom resource instances will use this value as the `kind` attribute in API calls.
                    type: string
                  listKind:
                    description: listKind is the serialized kind of the list for this
                      resource. Defaults to "`kind`List".
                    type: string
                  plural:
                    description: |-
                      plural is the plural name of the resource to serve.
                      The custom resources are served under `/apis/<group>/<version>/.../<plural>`.
                      Must match the name of the CustomResourceDefinition (in the form `<names.plural>.<group>`).
                      Must be all lowercase.
                    type: string
                  shortNames:
                    description: |-
                      shortNames are short names for the resource, exposed in API discovery documents,
                      and used by clients to support invocations like `kubectl get <shortname>`.
                      It must be all lowercase.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  singular:
                    description: singular is the singular name of the resource. It
                      must be all lowercase. Defaults to lowercased `kind`.
                    type: string
                required:
                - kind
                - plural
                type: object
              provides:
                description: Provides is a list of dependencies that this CRD provides.
                items:
                  properties:
                    name:
                      description: |-
                        Name of the dependency.
                        Should be the fully qualified crd name as written in the .metadata.name field of the CRD.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              reference:
                description: Reference is a reference to the source of the bundle.
                properties:
                  apiVersion:
                    description: APIVersion of the referent.
                    enum:
                    - source.toolkit.fluxcd.io/v1
                    type: string
                  kind:
                    description: Kind of the referent.
                    enum:
                    - OCIRepository
                    type: string
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
              requires:
                description: Requires is a list of dependencies that this CRD requires.
                items:
                  properties:
                    name:
                      description: |-
                        Name of the dependency.
                        Should be the fully qualified crd name as written in the .metadata.name field of the CRD.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              versionDiscovery:
                description: VersionDiscovery defines how to discover the version
                  of the source.
                properties:
                  reference:
                    description: Reference is a reference to the discovery source
                      of the version.
                    properties:
                      apiVersion:
                        description: APIVersion of the referent.
                        enum:
                        - image.toolkit.fluxcd.io/v1
                        type: string
                      kind:
                        description: Kind of the referent.
                        enum:
                        - ImageRepository
                        type: string
                      name:
                        description: Name of the referent.
                        minLength: 1
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                required:
                - reference
                type: object
            required:
            - reference
            type: object
          status:
            description: CustomResourceDefinitionSourceStatus defines the observed
              state of CustomResourceDefinitionSource
            properties:
              appliedReferenceGeneration:
                description: AppliedReferenceGeneration is the last applied generation
                  of the referenced source.
                format: int64
                type: integer
              appliedReferenceRevision:
                description: AppliedReferenceRevision is the last applied revision
                  of the referenced source.
                type: string
              conditions:
                description: Conditions holds the conditions for the CustomResourceDefinitionSource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
